// const frutas = ['Pera', 'Maçã', 'Uva'];

// for (let i in frutas) {
//     console.log(frutas[i])
// }

const namorada = {
    nome: 'Fernanda',
    sobrenome: 'Naves',
    idade: 20
}

for (let i in namorada) {
    console.log(`${i}:`, namorada[i]);
}